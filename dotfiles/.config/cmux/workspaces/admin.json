{
  "name": "admin",
  "cwd": "~",
  "panes": [
    {"command": "agentsview update --yes && agentsview serve --no-browser"},
    {"split": "right", "command": "cd ~/code/kenn-forge && git pull origin main && mise install && make install && kenn-forge daemon start"}
  ]
}
