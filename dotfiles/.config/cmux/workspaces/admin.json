{
  "name": "admin",
  "cwd": "~",
  "panes": [
    {"command": "agentsview update --yes && agentsview serve --no-browser"},
    {"split": "right", "command": "cd ~/code/kenn-forge && git pull origin main && mise install && make build && make install && ./kenn-forge serve"}
  ]
}
