# Case Insensitive Globbing
setopt NO_CASE_GLOB

# ask to auto correct command
setopt CORRECT

# Show command runtime, if longer than 30 seconds
export REPORTTIME=30

# Color output on macOS
export CLICOLOR=1

# Prompt confirmation for `rm *`
setopt RM_STAR_WAIT
