name: admin
root: ~
windows:
  - agentsview: agentsview update --yes && agentsview serve --no-browser
  - kenn-forge:
      root: ~/code/kenn-forge
      panes:
        - git pull origin main && mise install && make install && kenn-forge daemon start
