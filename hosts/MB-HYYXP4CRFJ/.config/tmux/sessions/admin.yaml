name: admin
root: ~
windows:
  - agentsview: agentsview update --yes && agentsview serve --no-browser
  - kenn-forge:
      root: ~/code/kenn-forge
      panes:
        - git pull origin main && mise install && make build && make install && ./kenn-forge serve
