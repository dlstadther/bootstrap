name: admin
root: ~
windows:
  - agentsview: agentsview update && agentsview serve
  - middleman:
      root: ~/code/middleman
      panes:
        - git pull origin main && mise install && make install && middleman
